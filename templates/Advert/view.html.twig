{% extends "Advert/layout.html.twig" %}

{% block title %}
  Lecture d'une annonce - {{ parent() }}
{% endblock %}

{% block ocplatform_body %}

  <h2>{{ advert.title }}</h2>
  <i>Par {{ advert.author }}, le {{ advert.date|date('d/m/Y') }}</i>

  <div class="well">
    {{ advert.content }}
  </div>

  <p>
    <a href="{{ path('oc_advert_index') }}" class="btn btn-primary">
      Retour à la liste
    </a>
    <a href="{{ path('oc_advert_edit', {'id': advert.id}) }}" class="btn btn-primary">
      Modifier l'annonce
    </a>
    <a href="{{ path('oc_advert_delete', {'id': advert.id}) }}" class="btn btn-danger">
      Supprimer l'annonce
    </a>
  </p>

{% endblock %}